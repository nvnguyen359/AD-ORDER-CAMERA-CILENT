import { StatusPipesPipe } from './status-pipes-pipe';

describe('StatusPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
